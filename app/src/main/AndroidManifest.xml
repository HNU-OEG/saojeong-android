<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.saojeong">

    <application
        android:allowBackup="true"
        android:icon="@drawable/logo_no_circle"
        android:label="@string/app_name"
        android:roundIcon="@drawable/logo_no_circle"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">
        android:windowSoftInputMode="adjustResize"
        <activity android:name=".IntroPage">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".CommunityWirteActivity"></activity>
        <activity android:name=".CommunityReadActivity"></activity>
        <activity
            android:name=".MainActivity"
            android:windowSoftInputMode="adjustNothing"
            >

        </activity>

    </application>

</manifest>